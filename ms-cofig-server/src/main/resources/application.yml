server:
  port: 7070
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JosueGabriel0/DAD-2024-22.git # ruta del repositorio git
          searchPaths: config-data
          default-label: config-server
  security:
    user:
      name: root
      password: 123456